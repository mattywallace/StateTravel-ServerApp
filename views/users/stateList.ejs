<%- include('../partials/header.ejs') %>

<h3> <%= user.user %>'s States Visited</h3>

	<ul>
		<% states.forEach((state) => { %>
			<li>
		 		<a href="/states/<%= state.id %>"><%= state.states %></a>
			</li>
		<%})%>
	</ul>


<%- include('../partials/footer.ejs') %>

